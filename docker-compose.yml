---
services:
  acestream:
    container_name: acestream
    image: ghcr.io/krinkuto11/acestream-http-proxy:latest
    restart: unless-stopped
    ports:
      - "6879:6879"
    environment:
      HTTP_PORT: 6879
      HTTPS_PORT: 6880
      BIND_ALL: "true"
